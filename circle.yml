dependencies:
  pre:
    - make deps
  override:
    - godep restore

test:
  override:
    - godep go test -v ./...
  post:
    - make build
    - cp -r build $CIRCLE_ARTIFACTS/

deployment:
  release:
    branch: master
    commands:
      - make release
